node_modules/
dist/
*.log
.DS_Store

# 运行时本地状态（含本机路径与会话内容，不入库）
data/
*.tmp

# 实现期临时笔记（可丢，不入库）
.plans/

# Claude Code 本机记忆 / 本地设置（含本机路径，不入库）
.claude/

# 多特性并行开发的 worktree 与端口标记（见 specs/014）
.worktrees/
.commander-port

# impeccable skill 的本机安装/运行产物（工具环境，不入库）
.agents/
.impeccable/
skills-lock.json
